import 'package:flutter/material.dart';

class LearnSkills extends StatelessWidget {
  const LearnSkills({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text('learn skills')
      ],
    );
  }
}
import 'package:flutter/material.dart';

class HomeMobileLayoutBody extends StatelessWidget {
  const HomeMobileLayoutBody({super.key});

  @override
  Widget build(BuildContext context) {
    return  Column(
      children: [],
    );
  }
}
import 'package:flutter/widgets.dart';

class SkillsDetailsBody extends StatelessWidget {
  const SkillsDetailsBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Column();
  }
}